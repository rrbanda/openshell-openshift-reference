apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - external-secrets.yaml

# Helm chart is deployed separately by ArgoCD or via helm install.
# This kustomization handles namespace, SCC, and ESO resources only.
